# Bandit Level 28 → 29

## Goal
Clone the repository and inspect its Git history because the current README hides the useful value.

## Solution path

```bash
git clone ssh://bandit28-git@bandit.labs.overthewire.org:2220/home/bandit28-git/repo
cd repo
git log --oneline -p
```

## What I learned
Git history and recovering removed/changed content.

## Note
This file intentionally does not contain passwords. Use the live OverTheWire challenge to obtain them.
